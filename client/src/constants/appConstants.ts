export const USER_ROLES = ['user', 'provider'] as const;
export const BOOKING_STATUS = ['pending', 'accepted', 'completed', 'cancelled'] as const;
export const PRICE_TYPES = ['hour', 'fixed'] as const;